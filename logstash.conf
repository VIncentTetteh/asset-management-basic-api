input {
  tcp {
    port => 8009
    mode => "server"
    codec => json_lines { charset => "UTF-8" }
  }
}

# For quick verification, print incoming events to stdout.
# Replace this with your real output (e.g., Elasticsearch) when ready.
output {
  stdout { codec => rubydebug }
}
