# Stamp @lombok.Generated on all Lombok-generated methods so JaCoCo excludes
# them from coverage — getters/setters/builders are not meaningful test targets.
config.stopBubbling = true
lombok.addLombokGeneratedAnnotation = true
